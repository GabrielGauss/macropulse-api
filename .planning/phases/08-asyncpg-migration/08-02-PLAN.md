---
phase: 08-asyncpg-migration
plan: "02"
type: execute
wave: 3
depends_on:
  - "08-00"
  - "08-01"
files_modified:
  - api/routes/auth.py
  - api/routes/billing.py
  - api/middleware/auth_rate_limit.py
  - api/middleware/rate_limit.py
  - api/auth.py
  - pytest.ini
  - requirements.txt
  - tests/conftest.py
  - tests/test_auth_rate_limit.py
autonomous: true
requirements:
  - DB-10
  - DB-11
  - DB-13

must_haves:
  truths:
    - "All 7 route handlers in api/routes/auth.py are async def"
    - "check_auth_rate_limit() in auth_rate_limit.py is async def and awaited at call sites"
    - "_set_backoff_if_needed() in auth_rate_limit.py is async def"
    - "_resolve_limit() in rate_limit.py is async def and awaited in dispatch()"
    - "get_usage_today() in rate_limit.py is async def"
    - "_lookup_key() in api/auth.py is async def and awaited"
    - "No get_sync_cursor import exists anywhere in the API codebase"
    - "pytest.ini has asyncio_mode = auto"
    - "conftest.py mock_auth_rl_cursor patches get_db_conn with AsyncMock"
    - "test_auth_rate_limit.py patches database.queries.get_db_conn with AsyncMock"
    - "Full test suite passes: pytest exits 0"
  artifacts:
    - path: "api/routes/auth.py"
      provides: "7 async route handlers calling await queries.*"
    - path: "api/middleware/auth_rate_limit.py"
      provides: "async check_auth_rate_limit, async _set_backoff_if_needed"
    - path: "api/middleware/rate_limit.py"
      provides: "async _resolve_limit, async get_usage_today"
    - path: "api/auth.py"
      provides: "async _lookup_key"
    - path: "pytest.ini"
      provides: "asyncio_mode = auto"
    - path: "tests/conftest.py"
      provides: "mock_auth_rl_cursor using AsyncMock + get_db_conn"
    - path: "tests/test_auth_rate_limit.py"
      provides: "test_state_is_db_not_memory patches get_db_conn with AsyncMock"
  key_links:
    - from: "api/routes/auth.py register()"
      to: "check_auth_rate_limit()"
      via: "await check_auth_rate_limit(...)"
      pattern: "await check_auth_rate_limit"
    - from: "api/middleware/rate_limit.py dispatch()"
      to: "_resolve_limit()"
      via: "await _resolve_limit(...)"
      pattern: "await _resolve_limit"
    - from: "tests/conftest.py"
      to: "database.connection.get_db_conn"
      via: "patch('database.connection.get_db_conn', AsyncMock(...))"
      pattern: "AsyncMock"
---

<objective>
Wire the async query layer into all call sites: route handlers, middleware,
and the auth dependency. Update tests to use AsyncMock and pytest-asyncio.
Run the full test suite and confirm green.

Purpose: Plans 08-00 and 08-01 created the async infrastructure. This plan
connects it everywhere it's called and validates the migration end-to-end.

Output:
  - api/routes/auth.py — all handlers async def, await queries.*
  - api/routes/billing.py — remaining sync helpers async
  - api/middleware/auth_rate_limit.py — async check_auth_rate_limit
  - api/middleware/rate_limit.py — async _resolve_limit + get_usage_today
  - api/auth.py — async _lookup_key
  - pytest.ini — asyncio_mode = auto
  - requirements.txt — pytest-asyncio added
  - tests/conftest.py — AsyncMock fixtures
  - tests/test_auth_rate_limit.py — AsyncMock patches
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-asyncpg-migration/08-01-SUMMARY.md

@api/routes/auth.py
@api/routes/billing.py
@api/middleware/auth_rate_limit.py
@api/middleware/rate_limit.py
@api/auth.py
@tests/conftest.py
@tests/test_auth_rate_limit.py
@pytest.ini
@requirements.txt

<interfaces>
<!-- Contract from plan 08-01: all queries.* are now async def -->
<!-- Any call to queries.* must be: await queries.func(...) -->
<!-- check_auth_rate_limit() will become async — callers must await it -->

<!-- AsyncMock pattern for tests -->
from unittest.mock import AsyncMock, patch, MagicMock

# Patch async context manager (get_db_conn)
mock_conn = MagicMock()
mock_conn.fetchone.return_value = {"attempt_count": 1, "locked_until": None}
mock_cm = AsyncMock()
mock_cm.__aenter__ = AsyncMock(return_value=mock_conn)
mock_cm.__aexit__ = AsyncMock(return_value=False)
with patch("database.connection.get_db_conn", return_value=mock_cm):
    ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert route handlers and middleware to async</name>
  <files>
    api/routes/auth.py,
    api/routes/billing.py,
    api/middleware/auth_rate_limit.py,
    api/middleware/rate_limit.py,
    api/auth.py
  </files>
  <action>
Make every function that calls a queries.* function (now async) itself async def,
and add await at each call site. Specific changes per file:

--- api/routes/auth.py ---

Every route handler is currently sync def. Convert ALL 7 to async def:
  register, verify, recover, recover_verify, rotate_key, get_me, get_usage

For each handler:
  1. Change: def handler(...) -> async def handler(...)
  2. Add await before every queries.* call
  3. Add await before check_auth_rate_limit() (it will be async after this plan)

Example (register):
  BEFORE: def register(body, request):
              check_auth_rate_limit(...)
              queries.create_email_verification(email, code)
  AFTER:  async def register(body, request):
              await check_auth_rate_limit(...)
              await queries.create_email_verification(email, code)

recover_verify also has a direct get_sync_cursor call at lines 229–235:
  Remove the get_sync_cursor import
  The logic likely fetches user tier — convert to: user = await queries.get_user_by_id(user_id)
  (Read the actual code to determine exactly what is fetched and replicate with async queries)

--- api/routes/billing.py ---

Find all locations using get_sync_cursor directly or calling sync queries.*:
  - paddle_webhook handler: already async def — just add await before queries.* calls
  - _ls_provision() and _ls_handle() helper functions: make async def, add await

--- api/middleware/auth_rate_limit.py ---

_set_backoff_if_needed():
  1. Change: def → async def
  2. Change import: remove get_sync_cursor, the SQL is currently a direct execute
  3. The SQL in _set_backoff_if_needed uses %s placeholders — convert to $1/$2/$3
  4. Replace the with get_sync_cursor() block:
     BEFORE:
       sql = "UPDATE auth_rate_limits SET locked_until = now() + %s::interval WHERE identifier = %s AND endpoint = %s"
       with get_sync_cursor() as cur:
           cur.execute(sql, (interval_literal, identifier, endpoint))
     AFTER:
       sql = "UPDATE auth_rate_limits SET locked_until = now() + $1::interval WHERE identifier = $2 AND endpoint = $3"
       async with get_db_conn() as conn:
           await conn.execute(sql, interval_literal, identifier, endpoint)
  5. Add import: from database.connection import get_db_conn
  6. Remove import: from database.connection import get_sync_cursor

check_auth_rate_limit():
  1. Change: def → async def
  2. Change: result = queries.check_and_record_attempt(...) → result = await queries.check_and_record_attempt(...)
  3. Change: _set_backoff_if_needed(...) → await _set_backoff_if_needed(...)

--- api/middleware/rate_limit.py ---

_resolve_limit():
  1. Change: def _resolve_limit → async def _resolve_limit
  2. Change: record = get_api_key_by_hash(key_hash) → record = await get_api_key_by_hash(key_hash)
     (get_api_key_by_hash is now async from queries.py)

get_usage_today():
  1. Change: def → async def
  2. Change: return get_daily_usage(client_id) → return await get_daily_usage(client_id)

RateLimitMiddleware.dispatch():
  Already async def. Update:
  1. change: limit, key_hash = _resolve_limit(...) → limit, key_hash = await _resolve_limit(...)
  2. change: allowed = check_and_set_ip_lock(...) → allowed = await check_and_set_ip_lock(...)
  3. change: count = increment_daily_usage(...) → count = await increment_daily_usage(...)
  4. change: count = get_daily_usage(...) → count = await get_daily_usage(...)

--- api/auth.py ---

Read the file first. Find _lookup_key() which calls get_api_key_by_hash():
  1. Change: def _lookup_key → async def _lookup_key
  2. Change: record = queries.get_api_key_by_hash(key_hash) → record = await queries.get_api_key_by_hash(key_hash)
  3. require_api_key() is already async def — change _lookup_key() call to await _lookup_key()
  </action>
  <verify>
    <automated>cd c:/Users/gabri/OneDrive/Documentos/code/claude/macropulse && grep -rn "get_sync_cursor" api/ && echo "FAIL: legacy found" || echo "PASS: no get_sync_cursor in api/"</automated>
  </verify>
  <done>
    - No get_sync_cursor import in any file under api/
    - All 7 auth route handlers are async def
    - check_auth_rate_limit is async def
    - _resolve_limit is async def
    - _lookup_key is async def
    - python -c "from api.main import app" exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tests for AsyncMock and pytest-asyncio</name>
  <files>
    pytest.ini,
    requirements.txt,
    tests/conftest.py,
    tests/test_auth_rate_limit.py
  </files>
  <action>
Four targeted changes:

CHANGE 1 — pytest.ini: add asyncio_mode:
  Add one line under [pytest]:
    asyncio_mode = auto
  Final pytest.ini:
    [pytest]
    testpaths = tests
    python_files = test_*.py
    python_classes = Test*
    python_functions = test_*
    addopts = -q
    asyncio_mode = auto

CHANGE 2 — requirements.txt: add pytest-asyncio:
  Add after the existing pytest line (or create a test section if none exists):
    pytest-asyncio>=0.23,<1.0
  If pytest is not in requirements.txt (it may be in a separate dev section),
  add the line under the pytest section or at the end of the file.

CHANGE 3 — tests/conftest.py: update mock_auth_rl_cursor fixture:
  The fixture currently patches database.connection.get_sync_cursor with a
  synchronous MagicMock context manager. Replace entirely:

  BEFORE:
    @pytest.fixture()
    def mock_auth_rl_cursor():
        mock_row = {"attempt_count": 1, "locked_until": None}
        mock_cur = MagicMock()
        mock_cur.fetchone.return_value = mock_row
        mock_ctx = MagicMock()
        mock_ctx.__enter__ = MagicMock(return_value=mock_cur)
        mock_ctx.__exit__ = MagicMock(return_value=False)
        with patch("database.connection.get_sync_cursor", return_value=mock_ctx):
            yield mock_cur

  AFTER:
    @pytest.fixture()
    def mock_auth_rl_cursor():
        """Mock get_db_conn for auth_rate_limits queries (asyncpg)."""
        mock_row = {"attempt_count": 1, "locked_until": None}
        mock_conn = MagicMock()
        mock_conn.fetchrow = AsyncMock(return_value=mock_row)
        mock_conn.execute = AsyncMock(return_value=None)
        mock_cm = AsyncMock()
        mock_cm.__aenter__ = AsyncMock(return_value=mock_conn)
        mock_cm.__aexit__ = AsyncMock(return_value=False)
        with patch("database.connection.get_db_conn", return_value=mock_cm):
            yield mock_conn

  Also add AsyncMock to the imports at the top of conftest.py:
    from unittest.mock import AsyncMock, MagicMock, patch

CHANGE 4 — tests/test_auth_rate_limit.py: update test_state_is_db_not_memory:
  Find the test that patches database.queries.get_sync_cursor (line ~179).

  BEFORE:
    with patch("database.queries.get_sync_cursor", return_value=mock_ctx):
        check_and_record_attempt(...)
    assert mock_cur.execute.called

  AFTER:
    mock_conn = MagicMock()
    mock_conn.fetchrow = AsyncMock(return_value=mock_row)
    mock_conn.execute = AsyncMock(return_value=None)
    mock_cm = AsyncMock()
    mock_cm.__aenter__ = AsyncMock(return_value=mock_conn)
    mock_cm.__aexit__ = AsyncMock(return_value=False)
    with patch("database.connection.get_db_conn", return_value=mock_cm):
        import asyncio
        asyncio.run(check_and_record_attempt(
            identifier="1.2.3.4",
            endpoint="register",
            max_attempts=5,
            window_minutes=60,
        ))
    assert mock_conn.fetchrow.called or mock_conn.execute.called

  Also update any imports at the top of test_auth_rate_limit.py — add AsyncMock,
  remove any references to MagicMock sync context manager pattern for get_sync_cursor.

  Read tests/test_auth_rate_limit.py fully before editing to catch any other
  patches of get_sync_cursor or synchronous call patterns for async functions.
  </action>
  <verify>
    <automated>cd c:/Users/gabri/OneDrive/Documentos/code/claude/macropulse && python -m pytest tests/ -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pytest.ini has asyncio_mode = auto
    - requirements.txt has pytest-asyncio
    - conftest.py mock_auth_rl_cursor uses AsyncMock and patches get_db_conn
    - test_auth_rate_limit.py patches get_db_conn not get_sync_cursor
    - pytest exits 0 (all tests pass)
  </done>
</task>

</tasks>

<verification>
```bash
cd c:/Users/gabri/OneDrive/Documentos/code/claude/macropulse

# No legacy DB symbols anywhere in api/
grep -rn "get_sync_cursor\|psycopg2" api/ && echo "FAIL" || echo "PASS: api/ clean"

# No legacy DB symbols in tests/
grep -rn "get_sync_cursor" tests/ && echo "FAIL" || echo "PASS: tests/ clean"

# App imports cleanly
python -c "from api.main import app; print('PASS: app OK')"

# Full test suite
python -m pytest tests/ -q
```

Expected final output: all tests pass, 0 errors.
</verification>

<success_criteria>
- `grep -rn "get_sync_cursor" api/ tests/` returns 0 matches
- `grep -rn "psycopg2" api/ database/` returns 0 matches
- All 7 handlers in api/routes/auth.py are async def
- check_auth_rate_limit is async def
- _resolve_limit is async def
- pytest.ini has asyncio_mode = auto
- conftest.py mock_auth_rl_cursor uses AsyncMock
- `python -m pytest tests/` exits 0 — no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-asyncpg-migration/08-02-SUMMARY.md`
</output>
