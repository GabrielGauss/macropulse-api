---
phase: 06-secrets-webhooks-infra-hardening
plan: "01"
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - .gitignore
  - .env.example
  - docs/DEPLOYMENT.md
autonomous: false
requirements:
  - SEC-10
  - SEC-11
  - SEC-12
  - SEC-13

must_haves:
  truths:
    - "`git log --all -p -- .env | grep -E 'BREVO|MTA_SIGNING|FRED_API'` returns no output"
    - ".env does not appear in `git ls-files` output"
    - ".env.example documents LS_WEBHOOK_SECRET, LS_VARIANT_ID_STARTER, LS_VARIANT_ID_PRO, and ENV"
    - "docs/DEPLOYMENT.md documents secret rotation and environment injection procedure"
    - "All four secret categories (Brevo API key, MTA Ed25519 key pair, FRED API key, owner API key) confirmed rotated"
  artifacts:
    - path: ".env.example"
      provides: "Complete env var reference for deployers"
      contains: "LS_WEBHOOK_SECRET"
    - path: "docs/DEPLOYMENT.md"
      provides: "Secret rotation and env injection guide"
      contains: "git filter-repo"
  key_links:
    - from: "docs/DEPLOYMENT.md"
      to: ".env.example"
      via: "references env var names"
      pattern: "env.example"

user_setup:
  - service: brevo
    why: "Brevo API key must be rotated — old key is compromised via git history"
    env_vars:
      - name: BREVO_API_KEY
        source: "Brevo dashboard → Settings → API Keys → Generate new key; revoke the old one"
    dashboard_config:
      - task: "Revoke old API key"
        location: "Brevo dashboard → Settings → API Keys"
  - service: fred
    why: "FRED API key must be rotated — old key is compromised via git history"
    env_vars:
      - name: FRED_API_KEY
        source: "FRED website → My Account → API Keys → Request new key"
  - service: irl-engine
    why: "MTA Ed25519 key pair must be rotated and IRL Engine must receive the new public key before deploying"
    env_vars:
      - name: MTA_SIGNING_KEY_HEX
        source: "Generate new key pair: python -c \"from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PrivateKey; k=Ed25519PrivateKey.generate(); print(k.private_bytes_raw().hex())\""
    dashboard_config:
      - task: "Update MACROPULSE_PUBKEY_HEX in IRL Engine .env before deploying new MacroPulse private key"
        location: "IRL Engine server .env file"
---

<objective>
Purge all committed secrets from git history, rotate every compromised credential, and ensure .env.example documents every required variable with deployment guidance.

Purpose: Live Brevo API key and MTA Ed25519 private key are present in the git object store. Any repository clone or leak exposes these credentials. This plan removes them from history permanently and establishes documentation so future deployments never commit secrets again.
Output: Clean git history (no .env objects), rotated credentials, complete .env.example, DEPLOYMENT.md guide.
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-secrets-webhooks-infra-hardening/06-RESEARCH.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge .env from git history and lock .gitignore</name>
  <files>.gitignore</files>
  <action>
Run `git filter-repo` to permanently remove the .env file object from all commits in git history. Then verify the purge succeeded.

Steps:
1. Check if git-filter-repo is installed: `python -m git_filter_repo --version` — if missing, install with `pip install git-filter-repo`
2. Check for a remote: `git remote -v` — note the remote URL for the force-push step
3. Run the purge: `git filter-repo --path .env --invert-paths --force`
4. Verify .env is gone from history: `git log --all --full-history -- .env` must return no output
5. Verify .env is not tracked: `git ls-files .env` must return empty
6. Verify .gitignore already has .env: `grep '^\.env$' .gitignore` — if missing, add `.env` on its own line
7. If a remote exists, force-push to sync: `git push --force --all && git push --force --tags`
   - Document in DEPLOYMENT.md that the production server requires `git fetch --all && git reset --hard origin/main` after this force-push (not `git pull` — the histories have diverged)

DO NOT re-add .env or any secret values anywhere in the codebase.
DO NOT use the deprecated `git filter-branch` command. Use only `git filter-repo`.

Note: `filter-repo` changes all commit SHAs from the first commit that touched .env onward. This is expected and correct. Any other local clones of this repo (e.g., production server) will need to re-clone or hard-reset after the force-push.
  </action>
  <verify>
    <automated>cd c:/Users/gabri/OneDrive/Documentos/code/claude/macropulse && git log --all --full-history -- .env | wc -l</automated>
  </verify>
  <done>
- `git log --all --full-history -- .env` outputs nothing (zero lines)
- `git ls-files .env` outputs nothing
- `git log --all -p -- .env | grep -E 'BREVO|MTA_SIGNING|FRED_API'` outputs nothing
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and complete .env.example; create DEPLOYMENT.md</name>
  <files>.env.example, docs/DEPLOYMENT.md</files>
  <action>
**Part A — .env.example audit (SEC-12)**

Read the current .env.example. Confirm it contains all required variables. Add the four missing variables identified in research — they are currently absent because they are read via os.getenv() in billing.py directly (bypassing Settings):

Add these four entries to .env.example in the "Billing — Lemon Squeezy" section (or create the section if absent):
```
# Lemon Squeezy webhook
LS_WEBHOOK_SECRET=          # HMAC secret from LS dashboard → Webhooks → signing secret
LS_VARIANT_ID_STARTER=      # Integer variant ID for Starter plan from LS dashboard
LS_VARIANT_ID_PRO=          # Integer variant ID for Pro plan from LS dashboard
```

Add this entry to the "Deployment" section (create if absent):
```
# Deployment environment — controls startup guards
# Values: development | production
# In production, LS_WEBHOOK_SECRET must be set and CORS_ORIGINS must not be '*'
ENV=development
```

Do NOT add real values. Every new entry must have a comment describing what it is and where to find it.

**Part B — DEPLOYMENT.md (SEC-13)**

Create docs/DEPLOYMENT.md (create the docs/ directory if it does not exist). The file must document:

1. **Secret management policy:** Secrets live only in .env on the server. Never commit .env. Use `env_file: .env` in docker-compose.yml (already configured).

2. **Provisioning secrets on a new server:**
   ```bash
   # On the production server
   cp .env.example .env
   # Edit .env and fill in all required values
   nano .env
   ```

3. **After git filter-repo (force-push):** Production server re-sync procedure:
   ```bash
   git fetch --all
   git reset --hard origin/main
   # Do NOT use git pull — histories have diverged after filter-repo
   ```

4. **Secret rotation procedure** for each credential type:
   - Brevo API key: revoke old → generate new → update .env → `docker-compose up -d api`
   - MTA Ed25519 key: generate new private key hex → update IRL Engine MACROPULSE_PUBKEY_HEX FIRST → then update MacroPulse .env → deploy both atomically to avoid signature gap
   - FRED API key: request new key at fred.stlouisfed.org → update .env → restart API
   - Owner API key: generate with `python -c "import secrets; print(secrets.token_urlsafe(32))"` → update .env → restart API

5. **Required env vars for production startup:** List that if any of the following are missing, the app will refuse to start (or log a warning in development): `MTA_SIGNING_KEY_HEX`, `LS_WEBHOOK_SECRET` (production only), `ENV=production` (to enable startup guards).
  </action>
  <verify>
    <automated>cd c:/Users/gabri/OneDrive/Documentos/code/claude/macropulse && grep -c "LS_WEBHOOK_SECRET" .env.example && grep -c "ENV=" .env.example && test -f docs/DEPLOYMENT.md && echo "OK"</automated>
  </verify>
  <done>
- .env.example contains LS_WEBHOOK_SECRET, LS_VARIANT_ID_STARTER, LS_VARIANT_ID_PRO, and ENV entries with descriptive comments
- docs/DEPLOYMENT.md exists and documents secret rotation for all four credential types
- No real secret values present in either file
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  - Task 1: git filter-repo purged .env from all history; force-push completed if remote exists
  - Task 2: .env.example updated with 4 missing variables; docs/DEPLOYMENT.md created with rotation procedures

  Secrets that must now be rotated (external actions required):
  1. Brevo API key — revoke old key in Brevo dashboard, generate new key, update production .env
  2. MTA Ed25519 key pair — generate new pair, update IRL Engine first (MACROPULSE_PUBKEY_HEX), then MacroPulse (MTA_SIGNING_KEY_HEX)
  3. FRED API key — request new key at fred.stlouisfed.org, update production .env
  4. Owner API key — generate with `python -c "import secrets; print(secrets.token_urlsafe(32))"`, update production .env
  </what-built>
  <how-to-verify>
  1. Verify git history is clean:
     ```bash
     git log --all -p -- .env | grep -E 'BREVO|MTA_SIGNING|FRED_API|OWNER_API_KEY'
     ```
     Expected: no output (empty result)

  2. Verify .env.example additions:
     ```bash
     grep -E 'LS_WEBHOOK_SECRET|LS_VARIANT_ID_STARTER|LS_VARIANT_ID_PRO|^ENV=' .env.example
     ```
     Expected: 4 matching lines

  3. Verify DEPLOYMENT.md exists:
     ```bash
     cat docs/DEPLOYMENT.md | head -20
     ```
     Expected: Deployment guide content visible

  4. Confirm external rotations:
     - New Brevo API key has been set in production .env and tested (send a test email or verify API responds)
     - New MTA Ed25519 public key has been deployed to IRL Engine
     - New FRED API key is active
     - New OWNER_API_KEY is set in production .env
  </how-to-verify>
  <resume-signal>Type "rotated" once all four secret rotations are complete and git history verified clean, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
git log --all -p -- .env | grep -E 'BREVO|MTA_SIGNING|FRED_API|OWNER_API_KEY'
Expected: empty output — no secret values in git history
</verification>

<success_criteria>
- git log --all --full-history -- .env returns nothing
- git log --all -p -- .env | grep -E 'BREVO|MTA_SIGNING|FRED_API|OWNER_API_KEY' returns nothing
- .env.example has LS_WEBHOOK_SECRET, LS_VARIANT_ID_STARTER, LS_VARIANT_ID_PRO, ENV entries
- docs/DEPLOYMENT.md exists with secret rotation procedures for all four credential types
- All four credentials rotated (confirmed by human checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/06-secrets-webhooks-infra-hardening/06-01-SUMMARY.md`
</output>
