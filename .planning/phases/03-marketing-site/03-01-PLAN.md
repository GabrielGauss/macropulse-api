---
phase: 03-marketing-site
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - site/index.html
autonomous: true
requirements:
  - SITE-01
must_haves:
  truths:
    - "The hero H1 reads exactly 'Stop predicting. Start allocating.' with 'allocating' rendered in green"
    - "The hero layout (narrative left, code terminal right, metrics bar, CTAs) is unchanged"
    - "Social meta tags match the new headline for brand consistency"
  artifacts:
    - path: "site/index.html"
      provides: "Updated hero H1 and meta tags"
      contains: "Stop predicting.<br/>Start <em>allocating.</em>"
  key_links:
    - from: "site/index.html line ~1240"
      to: ".hero h1 em CSS rule"
      via: "<em> tag inside <h1>"
      pattern: "<em>allocating"
---

<objective>
Replace the hero H1 headline with "Stop predicting. Start allocating." and update the matching social meta tags.

Purpose: Sharper contrarian framing positions MacroPulse against forecasting tools and resonates with the quant/developer audience. "Accordingly" was weak; "allocating" as the green action word is the correct replacement.
Output: site/index.html with updated H1 (line 1240) and og/twitter meta tags (lines 13, 22).
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-marketing-site/03-CONTEXT.md
@.planning/phases/03-marketing-site/03-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from site/index.html. No codebase exploration needed. -->

Hero H1 at line ~1240 (current):
```html
<h1>Know the regime.<br/>Allocate<br/><em>accordingly.</em></h1>
```

Target H1:
```html
<h1>Stop predicting.<br/>Start <em>allocating.</em></h1>
```

Green highlight CSS (lines 136-139):
```css
.hero h1 em {
  color: var(--green); font-style: normal;
  text-shadow: 0 0 40px rgba(34,197,94,0.35);
}
```

og:title meta tag at line ~13 (current):
```html
<meta property="og:title" content="MacroPulse — Know the Regime. Allocate Accordingly." />
```

twitter:title meta tag at line ~22 (current):
```html
<meta name="twitter:title" content="MacroPulse — Know the Regime. Allocate Accordingly." />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hero H1 and social meta tags</name>
  <files>site/index.html</files>
  <action>
Make three targeted text edits to site/index.html:

1. H1 at line ~1240: Replace
   `<h1>Know the regime.<br/>Allocate<br/><em>accordingly.</em></h1>`
   with
   `<h1>Stop predicting.<br/>Start <em>allocating.</em></h1>`

   Do NOT add an extra `<br/>` inside "Start allocating." — the old headline had three visual lines (Know / Allocate / accordingly); the new headline has exactly two clean sentence lines (Stop predicting. / Start allocating.). No other changes to the hero section.

2. og:title meta tag at line ~13: Replace content value with
   `MacroPulse — Stop Predicting. Start Allocating.`

3. twitter:title meta tag at line ~22: Replace content value with
   `MacroPulse — Stop Predicting. Start Allocating.`

Do NOT change:
- The hero layout (two columns, metrics bar, CTAs)
- The `<em>` CSS rule
- The hero subtext paragraph (lines 1241-1243) — discretionary, leave unchanged
- Any other H1 or heading in the file
- The `class="chart-filter active"` on any button (that is SITE-04, handled in plan 02)
  </action>
  <verify>
    <automated>grep -n "Stop predicting" C:/Users/gabri/OneDrive/Documentos/code/claude/macropulse/site/index.html</automated>
    Expect: line ~1240 matches `<h1>Stop predicting.` and lines ~13 and ~22 match the updated meta content.
    Also run: `grep -n "Know the regime\|Allocate Accordingly\|accordingly" C:/Users/gabri/OneDrive/Documentos/code/claude/macropulse/site/index.html`
    Expect: zero matches (old copy fully replaced).
  </verify>
  <done>
    - H1 reads exactly: `<h1>Stop predicting.<br/>Start <em>allocating.</em></h1>`
    - og:title and twitter:title both read: "MacroPulse — Stop Predicting. Start Allocating."
    - No occurrences of old headline text remain in the file
  </done>
</task>

</tasks>

<verification>
grep confirms new H1 text present and old H1 text absent. Social meta tags updated. No other hero markup changed.
</verification>

<success_criteria>
The hero section of site/index.html leads with "Stop predicting. Start allocating." — "allocating" is wrapped in `<em>` and renders green. The two-column hero layout is intact. Social sharing meta tags reflect the new headline.
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketing-site/03-01-SUMMARY.md` following the summary template.
</output>
