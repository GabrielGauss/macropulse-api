---
phase: 03-marketing-site
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - site/index.html
autonomous: true
requirements:
  - SITE-04
must_haves:
  truths:
    - "The macro regime calendar renders 180 days of data on initial page load"
    - "The '6M' button appears with the active CSS class on page load; '3M' does not"
    - "Clicking '3M' switches to 90-day view; clicking '6M' restores 180-day view"
  artifacts:
    - path: "site/index.html"
      provides: "Calendar default updated to 6-month view"
      contains: "renderRegimeCalendar(_chartData, 180)"
  key_links:
    - from: "site/index.html line ~1566 button active class"
      to: "renderRegimeCalendar call at line ~2363"
      via: "HTML active state must match the JS default argument"
      pattern: "data-cal-range=\"180\".*active"
---

<objective>
Change the macro regime calendar default view from 90 days (3 months) to 180 days (6 months) by updating the JS call argument and the button active state in HTML.

Purpose: A 6-month default gives visitors a richer signal history on first view, matching SITE-04 requirement that the calendar "opens in a 6-month centered view by default."
Output: site/index.html with updated renderRegimeCalendar call (line 2363) and swapped button active class (lines 1566-1567).
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-marketing-site/03-CONTEXT.md
@.planning/phases/03-marketing-site/03-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from site/index.html. No codebase exploration needed. -->

Calendar JS call at line ~2363 (current):
```javascript
renderRegimeCalendar(_chartData, 90);
```

Target:
```javascript
renderRegimeCalendar(_chartData, 180);
```

Calendar filter buttons at lines ~1566-1567 (current):
```html
<button class="chart-filter active" data-cal-range="90"  onclick="setCalRange(90)">3M</button>
<button class="chart-filter" data-cal-range="180" onclick="setCalRange(180)">6M</button>
```

Target (swap `active` to the 6M button):
```html
<button class="chart-filter" data-cal-range="90"  onclick="setCalRange(90)">3M</button>
<button class="chart-filter active" data-cal-range="180" onclick="setCalRange(180)">6M</button>
```

Note: `setCalRange()` function at lines ~2391-2396 is correct and unchanged — it handles active state when the user clicks. Only the initial HTML attribute and the default JS argument need updating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update calendar default to 180 days and swap button active state</name>
  <files>site/index.html</files>
  <action>
Make two targeted edits to site/index.html:

1. JS call at line ~2363: Replace
   `renderRegimeCalendar(_chartData, 90);`
   with
   `renderRegimeCalendar(_chartData, 180);`

2. Button active class at lines ~1566-1567: Remove `active` from the 3M button and add it to the 6M button:
   - Change `<button class="chart-filter active" data-cal-range="90"  onclick="setCalRange(90)">3M</button>`
     to `<button class="chart-filter" data-cal-range="90"  onclick="setCalRange(90)">3M</button>`
   - Change `<button class="chart-filter" data-cal-range="180" onclick="setCalRange(180)">6M</button>`
     to `<button class="chart-filter active" data-cal-range="180" onclick="setCalRange(180)">6M</button>`

Do NOT change:
- The `setCalRange()` function body
- Any other occurrence of `chart-filter active` in the file (other chart sections may have their own filter buttons)
- The scroll-to-today logic inside `renderRegimeCalendar`
- Any performance chart button groups that are unrelated to the calendar section
  </action>
  <verify>
    <automated>grep -n "renderRegimeCalendar(_chartData, 180)" C:/Users/gabri/OneDrive/Documentos/code/claude/macropulse/site/index.html</automated>
    Expect: exactly one match at line ~2363.
    Also run: `grep -n "renderRegimeCalendar(_chartData, 90)" C:/Users/gabri/OneDrive/Documentos/code/claude/macropulse/site/index.html`
    Expect: zero matches (old 90-day default fully replaced).
    Also run: `grep -n "data-cal-range=\"180\"" C:/Users/gabri/OneDrive/Documentos/code/claude/macropulse/site/index.html`
    Expect: the 6M button line contains `active` class.
  </verify>
  <done>
    - `renderRegimeCalendar(_chartData, 180)` present; `renderRegimeCalendar(_chartData, 90)` absent
    - The 6M button (`data-cal-range="180"`) has class `chart-filter active`
    - The 3M button (`data-cal-range="90"`) has class `chart-filter` only (no `active`)
  </done>
</task>

</tasks>

<verification>
grep confirms the JS call uses 180, the 6M button has the active class, and the 3M button does not.
</verification>

<success_criteria>
On page load the macro regime calendar renders 180 days of regime history with the "6M" button visually highlighted. Clicking "3M" switches to 90-day view and clicking "6M" restores 180-day view (setCalRange handles this correctly without any changes).
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketing-site/03-02-SUMMARY.md` following the summary template.
</output>
